function differentTermsOfInput(boxes, boxes2){
    boxes.forEach(function(boxes){
        console.log(boxes)
    });

    boxes2.forEach(function(boxes){
        console.log(boxes)
    })
};

// O(n + m) beecause there are two different arrays. 
// Rule of thumb - same indentation + ---- nested indentation *