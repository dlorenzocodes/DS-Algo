
const array = ['box1', 'box2', 'box3'];
function grabBox(array){
    for( let i = 0; i < array.length; i++){
        console.log(array[0])
    }
}

// O(1) - Contant Time 
// Always takes the same time no matter the number of elements
// because it will always grab the first element in the array